# NubesGen configuration for App Service and VNet

This repository contains the files that should be generated by NubesGen, when
using App Service + Java + App Insights + Key Vault + PostgreSQL + VNet.
